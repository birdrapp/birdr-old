<%= simple_form_for([:admin, @bird_record]) do |f| %>
  <%= f.error_notification %>

    <%= f.association :bird %>
    <%= f.input :count %>
    <%= f.input :notes %>
    <%= f.association :birding_session %>

  <%= f.button :submit %>
<% end %>
