<%= simple_form_for([:admin, @localized_bird]) do |f| %>
  <%= f.error_notification %>

    <%= f.input :common_name %>
    <%= f.association :bird %>
    <%= f.input :locale %>
    <%= f.input :sort_position %>

  <%= f.button :submit %>
<% end %>
