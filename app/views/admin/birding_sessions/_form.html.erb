<%= simple_form_for([:admin, @birding_session]) do |f| %>
  <%= f.error_notification %>

    <%= f.input :date %>
    <%= f.input :time %>
    <%= f.input :location_name %>
    <%= f.input :location, as: :string %>
    <%= f.association :user %>

  <%= f.button :submit %>
<% end %>
