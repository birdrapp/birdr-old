<%= simple_form_for([:admin, @trip]) do |f| %>
  <%= f.error_notification %>

    <%= f.input :date %>
    <%= f.input :location, as: :string %>
    <%= f.association :user %>

  <%= f.button :submit %>
<% end %>
